interface:
  display_name: "Explodex Plugin Builder"
  short_description: "Create Explodex plugins, live when available"
  default_prompt: "Use $explodex-plugin-builder to create or modify an Explodex plugin. Use an existing Explodex renderer for live iteration when available; otherwise build and validate offline."

policy:
  allow_implicit_invocation: true
