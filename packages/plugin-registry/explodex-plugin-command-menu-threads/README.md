# Threads in Command Menu

Adds matching threads ahead of native commands in the command menu while leaving native command results intact. It preserves result limits, minimum query length, sort order, recent-on-open behavior, and the legacy persisted settings key.
