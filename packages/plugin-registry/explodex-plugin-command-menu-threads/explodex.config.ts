import { defineConfig } from "@explodex/sdk";

export default defineConfig({
  version: "1.1.0",
  displayName: "Threads in Command Menu",
  description:
    "Find any thread from the command menu, including threads inside collapsed projects.",
  lifecycle: "dynamic",
});
