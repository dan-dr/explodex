import { defineConfig } from "@explodex/sdk";

export default defineConfig({
  version: "1.6.0",
  displayName: "Feature Flags Playground",
  description:
    "Toggle experimental feature flags from Settings with persisted propagation.",
  lifecycle: "dynamic",
});
