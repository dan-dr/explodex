# Usage and Reset Glance

Shows a compact sidebar row and detail popover using only `GET /wham/usage` and `GET /wham/rate-limit-reset-credits`. Templates and refresh settings persist under `explodex-usage-reset-glance`, with migration from `explodex-usage-reset-sidebar`.
