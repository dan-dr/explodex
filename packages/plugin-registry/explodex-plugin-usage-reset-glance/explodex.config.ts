import { defineConfig } from "@explodex/sdk";

export default defineConfig({
  version: "1.3.0",
  displayName: "Usage and Reset Glance",
  description:
    "Keep usage and credit-reset countdowns visible in the sidebar.",
  lifecycle: "dynamic",
});
