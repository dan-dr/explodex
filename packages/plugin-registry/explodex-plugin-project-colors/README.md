# Project Colors

Color-codes project folders and threads with configurable targets, visual styles, palette colors, auto-assignment, and per-project or per-thread overrides. Settings remain stored under `explodex-project-colors`.
