import { defineConfig } from "@explodex/sdk";

export default defineConfig({
  version: "1.5.0",
  displayName: "Project Colors",
  description:
    "Color-code projects and their threads in the sidebar so you can tell them apart at a glance.",
  lifecycle: "dynamic",
});
