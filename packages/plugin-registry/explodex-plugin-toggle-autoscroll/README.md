# Toggle Autoscroll

Adds an Auto-scroll toggle above the composer. Settings preserve per-thread state, default state, text visibility, and always-visible behavior under the persisted key `explodex-toggle-autoscroll`.
