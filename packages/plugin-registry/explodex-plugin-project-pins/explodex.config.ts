import { defineConfig } from "@explodex/sdk";

export default defineConfig({
  version: "1.3.2",
  displayName: "Project Pins",
  description:
    "Pin a thread to its project instead of globally, and keep it at the top.",
  lifecycle: "dynamic",
});
