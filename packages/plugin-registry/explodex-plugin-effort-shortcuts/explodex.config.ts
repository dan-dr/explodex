import { defineConfig } from "@explodex/sdk";

export default defineConfig({
  version: "2.3.0",
  displayName: "Effort Shortcuts",
  description:
    "Set reasoning effort from composer prefixes and restore it after the turn.",
  lifecycle: "dynamic",
});
